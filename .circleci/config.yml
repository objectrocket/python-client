version: 2
jobs:
  test:
    docker:
      - image: python:3.7
    working_directory: ~/python-client
    steps:
      - checkout

      - run:
          command: pip install --upgrade pip tox

      - run:
          command: |
            tox -e lint
            tox -e py35

workflows:
  version: 2
  test:
    jobs:
      - test